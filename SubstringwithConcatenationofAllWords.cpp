#include <iostream>
#include <vector>
#include <string>

using namespace std;

class Solution {
public:
    vector<int> findSubstring(string s, vector<string>& words) {
        
    }
};

int main ()
{
    Solution sol;
    vector<string> words;
    words.push_back("barfoothefoobarman");
    string s = {"foo","bar"};
    sol.findSubstring(s,words);
    return 0;
}